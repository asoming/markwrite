# 应用图标

使用内置 imagegen 生成，2026-09-11。

源图：`public/assets/app-icon.png`。Tauri 图标通过 `npx tauri icon public/assets/app-icon.png` 转换，保存在 `src-tauri/icons/`。

生成提示词：

> Use case: logo-brand. Asset type: Linux desktop application icon for a calm Markdown editor named Markwrite (墨页). Generate one production-quality square app icon, 1024x1024, transparent canvas outside the icon. Center a softly rounded square cobalt blue tile (#4361D9), occupying about 88% of the canvas. On the tile, a single elegant white folded sheet whose folded geometry subtly forms the letter M, suggesting a page and writing. Modern restrained dimensional craftsmanship, delicate paper fold shadow only, perfectly crisp silhouette, high contrast and readable at 32 pixels. Mostly flat, very subtle depth. No wordmark, no Chinese text, no letters outside the abstract M shape, no pencil, no extra objects, no watermark, no mockup, no surrounding scene. One icon only, centered front-on, not tilted.
